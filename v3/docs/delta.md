# Delta

`v2` に対して以下を追加します。

- `.data` セクションでの変数定義（`dq` による 8 バイト変数）
- `mov [addr], reg`（store）— レジスタからメモリへの書き込み
- `mov reg, [addr]`（load）— メモリからレジスタへの読み出し
- `lea` による配列先頭アドレスの取得
- `[base + index*scale]` アドレッシングモードによる配列アクセス
- GDB `x/1xg` によるメモリ内容の確認
