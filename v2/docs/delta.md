# Delta

`v1` に対して以下を追加します。

- `cmp reg, imm` — 即値との比較（フラグのみ更新）
- `cmp reg, reg` — レジスタ間の比較
- `jz` / `jnz` — ゼロフラグによる条件ジャンプ
- `jg` — 符号付き大小比較による条件ジャンプ
- `jmp` — 無条件ジャンプ
- ラベルによるジャンプ先指定
- ループパターン（カウントダウン）
